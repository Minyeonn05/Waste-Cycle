// server/src/controllers/authController.js

// 🚨 เราจงใจลบฟังก์ชัน registerUser, loginUser, getAuthStatus ออกทั้งหมด
// เพราะเราย้ายการจัดการ Auth ไปที่ Client-side (Firebase SDK)
// และย้าย getAuthStatus ไปที่ userController.js (ในชื่อ getMyProfile)
// ไฟล์นี้จึงไม่จำเป็นต้องมีอะไรอีก