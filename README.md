# ♻️ โครงการ Waste Cycle (Full-stack)

นี่คือโปรเจกต์ Full-stack สำหรับจัดการวงจรของเสีย (Waste Cycle) พัฒนาด้วยสถาปัตยกรรมแบบ **Monorepo** โดยแยกส่วน Front-end (Client) และ Back-end (Server) ออกจากกันอย่างชัดเจน

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### ฝั่ง Front-end (client/)
* **React** (พัฒนาด้วย Vite)
* **React Router:** สำหรับการจัดการหน้า (Page Routes)
* **Axios:** สำหรับยิง API ไปยัง Back-end
* **Firebase Client SDK:** สำหรับการยืนยันตัวตน (Authentication) และระบบแชท (Firestore Realtime)
* **Google Maps API** (ผ่าน `@vis.gl/react-google-maps`): สำหรับแสดงแผนที่และปักหมุด

### ฝั่ง Back-end (server/)
* **Node.js**
* **Express:** สำหรับสร้าง API Routes
* **Firebase Admin SDK:** สำหรับจัดการข้อมูลหลังบ้านและยืนยันสิทธิ์ (Protected Routes)
* **CORS:** สำหรับอนุญาตให้ Client เรียก API
* **Dotenv:** สำหรับจัดการ Environment Variables (Key ลับ)

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Development)

ในการรันโปรเจกต์นี้ คุณจะต้องติดตั้ง Dependencies และตั้งค่า Environment Variables (ไฟล์เก็บ Key) ทั้งในฝั่ง `client` และ `server`

### 1. ติดตั้ง Dependencies (ทำครั้งแรก)

คุณต้องติดตั้ง `node_modules` ใน **ทั้งสองโฟลเดอร์**

```bash
# 1. ติดตั้ง Dependencies ของ Server
cd server
npm install

# 2. กลับมาที่ root แล้วไปติดตั้ง Dependencies ของ Client
cd ../client
npm install

# 3. กลับมาที่ root เพื่อเตรียมรัน
cd ..